checking the packages downloaded in linux 

rpm -qa 

for specifc packages

rpm -qa | grep -i iftop

rpm -qa | grep -i nethogs

Nethogs and iftop are tools to monitor Network Throughput and it.

Ping and Netstat

netcat to check ports are open 

